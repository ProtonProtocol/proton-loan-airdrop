contracts:
  rewards:
    abi: ../rewards/rewards.abi
    name: rewards
    wasm: ../rewards/rewards.wasm
  token:
    abi: ../eosio.token/eosio.token.abi
    name: token
    wasm: ../eosio.token/eosio.token.wasm
options:
  # serverBaseUrl: 'http://3.96.155.129:8888'
  skipAuth: false

